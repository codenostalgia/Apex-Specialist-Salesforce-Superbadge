global class WarehouseSyncSchedule implements Schedulable, Database.AllowsCallouts {
    global void execute(SchedulableContext ctx) {
        System.enqueueJob(new WarehouseCalloutService());
    }
}

//Run below code in Anonymous Window

/*
WarehouseSyncSchedule scheduledjob = new WarehouseSyncSchedule();

String format = '0 0 1 * * ?';
String jobID = System.schedule('WarehouseSyncScheduleJob', format, scheduledjob);  
**/